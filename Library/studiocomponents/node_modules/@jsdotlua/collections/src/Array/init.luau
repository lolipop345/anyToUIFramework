--!strict
local ES7Types = require(script.Parent.Parent.Parent.Parent:FindFirstChild('luau-aliases'):FindFirstChild('@jsdotlua'):FindFirstChild('es7-types'))

export type Array<T> = ES7Types.Array<T>

return {
	concat = require(script:FindFirstChild('concat')),
	every = require(script:FindFirstChild('every')),
	filter = require(script:FindFirstChild('filter')),
	find = require(script:FindFirstChild('find')),
	findIndex = require(script:FindFirstChild('findIndex')),
	flat = require(script:FindFirstChild('flat')),
	flatMap = require(script:FindFirstChild('flatMap')),
	forEach = require(script:FindFirstChild('forEach')),
	from = require(script:FindFirstChild('from')),
	includes = require(script:FindFirstChild('includes')),
	indexOf = require(script:FindFirstChild('indexOf')),
	isArray = require(script:FindFirstChild('isArray')),
	join = require(script:FindFirstChild('join')),
	map = require(script:FindFirstChild('map')),
	reduce = require(script:FindFirstChild('reduce')),
	reverse = require(script:FindFirstChild('reverse')),
	shift = require(script:FindFirstChild('shift')),
	slice = require(script:FindFirstChild('slice')),
	some = require(script:FindFirstChild('some')),
	sort = require(script:FindFirstChild('sort')),
	splice = require(script:FindFirstChild('splice')),
	unshift = require(script:FindFirstChild('unshift')),
}
