--!strict

return {
	assign = require(script:FindFirstChild('assign')),
	entries = require(script:FindFirstChild('entries')),
	freeze = require(script:FindFirstChild('freeze')),
	is = require(script:FindFirstChild('is')),
	isFrozen = require(script:FindFirstChild('isFrozen')),
	keys = require(script:FindFirstChild('keys')),
	preventExtensions = require(script:FindFirstChild('preventExtensions')),
	seal = require(script:FindFirstChild('seal')),
	values = require(script:FindFirstChild('values')),
	-- Special marker type used in conjunction with `assign` to remove values
	-- from tables, since nil cannot be stored in a table
	None = require(script:FindFirstChild('None')),
}
