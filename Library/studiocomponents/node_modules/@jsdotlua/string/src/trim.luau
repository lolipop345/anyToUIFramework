--!strict

local trimStart = require(script.Parent:FindFirstChild('trimStart'))
local trimEnd = require(script.Parent:FindFirstChild('trimEnd'))

return function(source: string): string
	return trimStart(trimEnd(source))
end
