return {
	charCodeAt = require(script:FindFirstChild('charCodeAt')),
	endsWith = require(script:FindFirstChild('endsWith')),
	findOr = require(script:FindFirstChild('findOr')),
	includes = require(script:FindFirstChild('includes')),
	indexOf = require(script:FindFirstChild('indexOf')),
	lastIndexOf = require(script:FindFirstChild('lastIndexOf')),
	slice = require(script:FindFirstChild('slice')),
	split = require(script:FindFirstChild('split')),
	startsWith = require(script:FindFirstChild('startsWith')),
	substr = require(script:FindFirstChild('substr')),
	trim = require(script:FindFirstChild('trim')),
	trimEnd = require(script:FindFirstChild('trimEnd')),
	trimStart = require(script:FindFirstChild('trimStart')),
	-- aliases for trimEnd and trimStart
	trimRight = require(script:FindFirstChild('trimEnd')),
	trimLeft = require(script:FindFirstChild('trimStart')),
}
